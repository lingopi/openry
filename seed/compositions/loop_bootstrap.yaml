# ============================================================================
# loop_bootstrap — 播种阶段（测试版：已配第①②③④⑤步）
# ============================================================================
# 逐 sub_step 测试中：当前已配置 loop_seed_build 的完整五步链。
# ============================================================================
name: loop_bootstrap
version: "0.5-test-step5"
description: "测试第⑤步：路由到运行环节（套娃执行生成的 loop_target）"

big_steps:
  - ref: loop_seed_build
    on_success: done
    on_failure: abort
